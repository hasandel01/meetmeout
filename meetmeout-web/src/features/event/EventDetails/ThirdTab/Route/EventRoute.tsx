import React from 'react';

interface EventRouteProps {
    routeDescription?: string;
    mapUrl?: string;
}

const EventRoute: React.FC<EventRouteProps> = () => {
    return (
       <div>

       </div>
    );
};

export default EventRoute;